/*#include <iostream>

using namespace std;
const int MAX = 3; //use const can't chanage data in MAX

int main() {

	int  var[MAX] = { 10, 100, 200 };
	int  * ptr = var; // let us have array address in pointer.


	for (int i = 0; i < MAX; i++) {

		cout << "Address of var: " << i << " = " << ptr << endl;
		cout << "Value of var: " << i << " = "<< *ptr << endl;
	
		ptr++; 	// point to the next location
	}

	system("PAUSE");
	return 0;
}
*/
