/*#include <iostream>

using namespace std;

int main() {
	int  var;
	int  *ptr;
	int  **pptr;
	int  ***ppptr;

	var = 3000;

	// take the address of var
	ptr = &var;

	// take the address of ptr using address of operator &
	pptr = &ptr;

	// take the address of pptr using address of operator &
	ppptr = &pptr;

	// take the value using pptr
	cout << "Value of var :" << var << endl;
	cout << "Address of var :" << &var << endl;
	cout << "Value available at *ptr :" << *ptr << endl;
	cout << "Address available at *ptr :" << ptr << endl;
	cout << "Value available at **pptr :" << **pptr << endl;
	cout << "Address available at **pptr :" << *pptr << endl;
	cout << "Value available at ***ppptr :" << ***ppptr << endl;
	cout << "Address available at ***ppptr :" << **ppptr << endl;

	system("PAUSE");
	return 0;
}
*/