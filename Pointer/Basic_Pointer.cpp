/*#include <iostream>

// cout *ptr = look value for pointer
// cout ptr = look address for pointer
// cout var = look value for actual variable
// cout &var = look address for actual variable
// pointer not reserve a space in ram memory but as soul of actual variable

using namespace std;

int main() {
	int  var1;
	char var2[10];

	cout << "Address of var1 variable: ";
	cout << &var1 << endl; // Address var1 in memory (use operator & look variable address)

	cout << "Address of var2 variable: ";
	cout << &var2 << endl; // Address var2 in memory

	system("PAUSE");
	return 0;
}
*/
/*
#include <iostream>

using namespace std;

int main() {
	int  var = 20; // actual variable declaration.
	int  *ip = &var; // pointer variable equals store address of var 

	cout << "Address stored in &var variable: " << &var << endl;

	cout << "Address stored in ip variable: " << ip << endl;

	cout << "Value of var variable: " << var << endl;

	cout << "Value of *ip variable: " << *ip << endl;

	system("PAUSE");
	return 0;
}
*/