/*#include <iostream>

using namespace std;

long calculate(unsigned int num1, unsigned int num2); // Function declaration

int main() {

cout << "Calculate: " << calculate(400, 200) << endl; // Call function and print output
system("PAUSE");
return 0;
}

long calculate(unsigned int num1, unsigned int num2) { // return_type function_name( parameter list ) {body of the function }

int resualt;

resualt = num1 + num2;

return resualt;
}
*/
